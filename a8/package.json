{
  "name": "a8",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A8 — PDF report generator: query -> render -> store -> serve",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "playwright": "^1.49.1"
  }
}
